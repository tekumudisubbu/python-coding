L=[]
n=int(input())
L = list(map(int, input().split()))
for i in range(0, n):
    for j in range(0, n):
        if L[i] > L[j]:
            x = L[i]
            L[i] = L[j]
            L[j] = x
print('Descending order of given list is:', L)
